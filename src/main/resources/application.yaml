spring:
  application:
    name: BlogManagement

  datasource:
    driver-class-name: org.postgresql.Driver
    password: Nicole0509
    username: postgres
    url: jdbc:postgresql://localhost:5432/blog_management

  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8081